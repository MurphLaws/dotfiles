---
phase: quick
plan: 260705-vx2
type: execute
autonomous: true
---

<objective>
Remove all transparency from the visual stack: Ghostty background, Neovim editor panes, and Tmux status bar/panes. Result: opaque, solid-color backgrounds throughout (Catppuccin Mocha @thm_bg: #1e1e2e).

Purpose: User requested "remove all transparency from nvim, ghostty, tmux, everything no transparency"
Output: Fully opaque terminal and editor, no blur effects, clean tree
</objective>

<execution_context>
@/Users/illico/dotfiles/CLAUDE.md
@/Users/illico/dotfiles/.planning/STATE.md
</execution_context>

<context>
## Current State

- **Ghostty**: `background-opacity = 0.88` + `background-blur-radius = 20` (lines 42–44)
- **Neovim**: `transparent_background = true` + 40-group transparency list + `apply_transparency()` autocmd (colorscheme.lua lines 9, 23–93)
- **Tmux**: Multiple `bg=default` across status bar, pane borders, window status (lines 105, 113, 116, 134, 137, 140, 141, 144, 150–152, 157–159)

Files involved:
- `ghostty/.config/ghostty/config`
- `nvim/.config/nvim/lua/illico/plugins/colorscheme.lua`
- `tmux/.tmux.conf`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Ghostty transparency and blur</name>
  <files>ghostty/.config/ghostty/config</files>
  <action>
    Set `background-opacity = 1` (change line 43 from 0.88 to 1). Remove `background-blur-radius = 20` (delete line 44 entirely). Update the comment block (lines 39–44) to remove references to translucency and blur being visible; replace with a simple note that the background is now fully opaque.
    
    Verify: `grep -E "background-opacity|background-blur" ghostty/.config/ghostty/config` should show opacity=1 only, no blur-radius line.
  </action>
  <verify>
    <automated>grep "background-opacity" ghostty/.config/ghostty/config | grep -q "1$" && ! grep -q "background-blur-radius" ghostty/.config/ghostty/config && echo "✓ Ghostty opacity=1, no blur"</automated>
  </verify>
  <done>Ghostty background is fully opaque (opacity=1), blur effect removed, comment updated</done>
</task>

<task type="auto">
  <name>Task 2: Disable Neovim transparency and remove transparency machinery</name>
  <files>nvim/.config/nvim/lua/illico/plugins/colorscheme.lua</files>
  <action>
    Change line 9: `transparent_background = true` → `transparent_background = false`.
    
    Delete lines 23–93 (the entire `transparent_groups` list, `apply_transparency()` function, and the `ColorScheme` autocmd). This includes:
    - Lines 23–64: `local transparent_groups = { ... }`
    - Lines 66–93: Function definition + autocmd + initial `apply_transparency()` call
    
    Keep everything else intact: fillchars (line 20), org/neorg highlight overrides (lines 115–132), palette exposure to globals (lines 134–152). The LineNr overrides (lines 86–88) will be deleted as part of the `apply_transparency()` function deletion — they were only needed with transparent bg.
    
    Result: Catppuccin Mocha with default (opaque) highlights, no custom transparency enforcement.
  </action>
  <verify>
    <automated>nvim --headless -c "colorscheme catppuccin-mocha" -c "echo (nvim_get_hl(0, {name: 'Normal'})).bg" -c "q" 2>&1 | grep -q "0" && echo "✓ Normal has bg (not NONE)"</automated>
  </verify>
  <done>Colorscheme config uses transparent_background=false, transparency list/function/autocmd removed, org/neorg/palette configs preserved</done>
</task>

<task type="auto">
  <name>Task 3: Replace Tmux bg=default with opaque theme colors</name>
  <files>tmux/.tmux.conf</files>
  <action>
    Replace all `bg=default` with `bg=#{@thm_bg}` (Catppuccin Mocha background: #1e1e2e) in the following lines:
    
    - Line 105: `status-style "bg=default"` → `"bg=#{@thm_bg}"`
    - Line 113: `pane-active-border-style "bg=default,fg=...` → `"bg=#{@thm_bg},fg=...`
    - Line 116: `pane-border-style "bg=default,fg=...` → `"bg=#{@thm_bg},fg=...`
    - Line 134: `window-status-format "#[bg=default,fg=...` → `"#[bg=#{@thm_bg},fg=...`
    - Line 137: `window-status-current-format "#[bg=default,fg=...` → `"#[bg=#{@thm_bg},fg=...`
    - Line 140: `window-status-style "bg=default,fg=...` → `"bg=#{@thm_bg},fg=...`
    - Line 141: `window-status-current-style "bg=default,fg=...` → `"bg=#{@thm_bg},fg=...`
    - Line 144: `window-status-separator "#[bg=default,fg=...` → `"#[bg=#{@thm_bg},fg=...`
    - Line 150: `status-left "#[bg=default,fg=#{@thm_blue}]` → `"#[bg=#{@thm_bg},fg=#{@thm_blue}]`
    - Line 151: `status-left "#[bg=default,fg=#{@thm_overlay_0}...` → `"#[bg=#{@thm_bg},fg=#{@thm_overlay_0}...`
    - Line 152: `status-left "#[bg=default,fg=#{@thm_yellow}]` → `"#[bg=#{@thm_bg},fg=#{@thm_yellow}]`
    - Line 157: The second bg (inside the non-prefix branch): `"#{#[bg=default,fg=#{@thm_green}]` → `"#{#[bg=#{@thm_bg},fg=#{@thm_green}]`
    - Line 158: `status-right "#[bg=default,fg=#{@thm_overlay_0}...` → `"#[bg=#{@thm_bg},fg=#{@thm_overlay_0}...`
    - Line 159: `status-right "#[bg=default,fg=#{@thm_blue}]` → `"#[bg=#{@thm_bg},fg=#{@thm_blue}]`
    
    Lines with conditional colors (e.g., activity, bell) that already use `bg=#{@thm_*}` are left unchanged.
  </action>
  <verify>
    <automated>! grep -n "bg=default" tmux/.tmux.conf | grep -v "^#" && echo "✓ No bg=default found (excluding comments)"</automated>
  </verify>
  <done>All Tmux status bar, pane borders, and window status elements now use bg=#{@thm_bg}, no default/transparent backgrounds</done>
</task>

</tasks>

<verification>
## Post-Execution Checks

1. **Ghostty**: Start Ghostty and verify background is solid opaque (not blurred). Check config file syntax: `ghostty --help-config | grep -i opacity`.
2. **Neovim**: Run `nvim --headless -c "colorscheme catppuccin-mocha" -c "echo 'OK'" -c "q"` — should not error. Visually: editor background is solid, not transparent to desktop.
3. **Tmux**: Source config with `tmux source-file tmux/.tmux.conf` (requires active session) or syntax check: `tmux start-server \; source-file tmux/.tmux.conf \; kill-server` (this initializes, loads, then exits). Status bar, pane borders, window tabs should all have solid #1e1e2e background.
</verification>

<success_criteria>
- Ghostty config has `background-opacity = 1` and no `background-blur-radius`
- Neovim colorscheme.lua has `transparent_background = false`, no transparency list, no apply_transparency function
- Tmux .tmux.conf has zero instances of `bg=default` (excluding comments)
- All three configs are syntactically valid (parseable by respective tools)
- Git tree clean after commits
</success_criteria>

<output>
Commits expected:
- `ghostty: opaco sin blur (background-opacity = 1)`
- `nvim: elimina transparencia en el editor (transparent_background = false)`
- `tmux: fondos opacos en barra y bordes (bg=#{@thm_bg})`

After: `/gsd-quick commit --all` or manual `git add .` + `git commit -m "..."`
</output>
