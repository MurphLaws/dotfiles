---
phase: 03-real-icons-verification
plan: 01
type: verify
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - ICONS-01
must_haves:
  truths:
    - "real-icons.nvim plugin is installed and the material pack is installed + rendered"
    - "ImageMagick (magick) is available for SVG pack rendering"
    - "tmux allow-passthrough is on so the Ghostty graphics protocol reaches nvim"
    - "real-icons.lua spec is correct (pack=material, backend=ghostty, fallback on, mini_files=false)"
  artifacts:
    - path: "nvim/.config/nvim/lua/illico/plugins/real-icons.lua"
      provides: "real-icons spec (verified, unchanged)"
---

<objective>
Verify the real-icons.nvim chain end-to-end at the code/config level (plugin, material pack, magick, tmux passthrough, fallback, integrations). This is an audit phase — modify code only if a broken link is found.
</objective>

<tasks>
<task type="verify">
  <name>Task 1: Audit the real-icons.nvim chain</name>
  <files>(read-only audit)</files>
  <read_first>
    nvim/.config/nvim/lua/illico/plugins/real-icons.lua; tmux/.tmux.conf; ~/.local/share/nvim/lazy/real-icons.nvim/; ~/.local/share/nvim/real-icons/packs/material/
  </read_first>
  <action>
Run system + config checks: magick present; tmux allow-passthrough on; plugin on disk; material pack installed and rendered (dist); spec keys correct. If every link is in order, document evidence and make NO code change. If a link is broken, apply the minimal fix.
  </action>
  <verify>
    <automated>command -v magick && grep -q "allow-passthrough on" tmux/.tmux.conf && test -d ~/.local/share/nvim/real-icons/packs/material</automated>
  </verify>
  <done>All chain links verified (see 03-01-SUMMARY.md). No code change required.</done>
</task>
</tasks>

<verification>
See 03-VERIFICATION.md — 7/7 chain links verified, status passed.
</verification>

<success_criteria>
- real-icons chain confirmed in order at code/config level (ICONS-01)
- icons render (material pack assets present; user screenshot confirms)
- no unrequested changes introduced
</success_criteria>

<output>
03-01-SUMMARY.md (results) and 03-VERIFICATION.md (verdict).
</output>
