---
phase: 04-tmux-claude-done-indicator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tmux/.tmux.conf
  - claude/.claude/hooks/tmux-claude-done.sh
  - claude/.claude/settings.json
autonomous: true
requirements:
  - NOTIFY-01
  - NOTIFY-02
must_haves:
  truths:
    - "tmux tab indicator uses bell glyph U+F0F3 (red) instead of U+F06A in both window-status formats"
    - "tmux-claude-done.sh hook fires on Notification in addition to Stop (reliable during GSD pauses)"
    - "hook script is versioned in repo at claude/.claude/hooks/ and symlinked from ~/.claude/hooks/"
    - "~/.claude/settings.json is versioned in repo at claude/.claude/settings.json and symlinked"
  artifacts:
    - path: "tmux/.tmux.conf"
      provides: "bell glyph in window-status-format and window-status-current-format"
    - path: "claude/.claude/hooks/tmux-claude-done.sh"
      provides: "versioned indicator hook"
    - path: "claude/.claude/settings.json"
      provides: "versioned hook registration (Stop + Notification)"
---

<objective>
Improve the tmux "Claude done / needs acknowledgement" indicator: bell glyph, fire on Notification + Stop, and version the hook + settings.json in the repo. Executed inline by the orchestrator (touches live ~/.claude).
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Bell glyph in tmux.conf</name>
  <files>tmux/.tmux.conf</files>
  <action>Replace the U+F06A glyph with U+F0F3 (bell) in window-status-format (line ~134) and window-status-current-format (line ~137), keeping fg=@thm_red and bold.</action>
  <verify><automated>grep -c $'' tmux/.tmux.conf</automated></verify>
</task>
<task type="auto">
  <name>Task 2: Version + relocate hook</name>
  <files>claude/.claude/hooks/tmux-claude-done.sh</files>
  <action>Copy the existing ~/.claude/hooks/tmux-claude-done.sh into claude/.claude/hooks/ (update header comment to mention "terminó o necesita acknowledgement"), then replace ~/.claude/hooks/tmux-claude-done.sh with a symlink to the repo copy (after backing up the original).</action>
  <verify><automated>test -L ~/.claude/hooks/tmux-claude-done.sh && test -f claude/.claude/hooks/tmux-claude-done.sh</automated></verify>
</task>
<task type="auto">
  <name>Task 3: Notification registration + version settings.json</name>
  <files>claude/.claude/settings.json</files>
  <action>Add tmux-claude-done.sh to the existing Notification hooks array in ~/.claude/settings.json (alongside peon-ping), keep it under Stop. Then copy settings.json into claude/.claude/settings.json and replace ~/.claude/settings.json with a symlink to the repo copy (after backup). Validate JSON.</action>
  <verify><automated>python3 -c "import json;json.load(open('claude/.claude/settings.json'))" && grep -c "Notification" claude/.claude/settings.json</automated></verify>
</task>
</tasks>

<verification>See 04-VERIFICATION.md.</verification>
<success_criteria>
- Bell glyph in both tmux formats; hook on Stop + Notification; hook + settings.json versioned and symlinked; valid JSON; tmux config valid.
</success_criteria>
<output>04-01-SUMMARY.md + 04-VERIFICATION.md</output>
