# Taskwarrior shell integration DISABLED
# Disabled per user request: keep user's existing ~/.task data untouched and
# avoid sourcing any taskwarrior hooks, aliases, or completions from dotfiles.
# If you later want this integration, restore the original file from the repo
# history or re-enable the lines below.
# (File intentionally left inert.)
