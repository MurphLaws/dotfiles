# Font Configuration
# font-family = JetBrainsMono NF SemiBold
font-family = Monocraft

# Keep your size, but Monocraft can be large, so adjust if needed
font-size = 20
font-thicken = true

# Theme
theme = 0x96f

# Transparency
background-opacity = 0.95
background-blur-radius = 20
keybind = shift+enter=text:\x1b\r
