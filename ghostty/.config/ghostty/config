# Font Configuration
font-family = JetBrainsMono Nerd Font Mono
font-style = Bold
font-style-bold = ExtraBold

font-size = 20
font-thicken = true

# Theme — 0x96f
background = 262427
foreground = fcfcfa
cursor-color = fcfcfa
cursor-text = 000000
selection-background = fcfcfa
selection-foreground = 262427

# ANSI palette (0x96f)
palette = 0=#262427
palette = 1=#ff666d
palette = 2=#b3e03a
palette = 3=#ffc739
palette = 4=#00cde8
palette = 5=#a392e8
palette = 6=#9deaf6
palette = 7=#fcfcfa
palette = 8=#545452
palette = 9=#ff7e83
palette = 10=#bee55e
palette = 11=#ffd05e
palette = 12=#1bd5eb
palette = 13=#b0a3eb
palette = 14=#acedf8
palette = 15=#fcfcfa

# Window — fullscreen no-nativo: "pantalla completa" es una ventana maximizada
# en el Space actual (no un Space aparte), así QuartoSplit puede dividirla.
macos-non-native-fullscreen = true

# Background fully opaque
#   No transparency, no blur — solid background color matching 0x96f.
background-opacity = 1

# Fondo con degradado en tonos 0x96f (PNG generado con ImageMagick:
#   magick -size 1920x1080 gradient:'#161417'-'#37333a' gradient-0x96f.png)
# background-image-opacity < 1 lo mezcla con el background 262427.
background-image = ~/.config/ghostty/backgrounds/gradient-0x96f.png
background-image-fit = cover
background-image-opacity = 0.7

# Shaders (GLSL) — método builtin de Ghostty (custom-shader)
#   1) CRT retro sutil (scanlines + bloom + viñeta, conserva Catppuccin)
#   2) animación de cursor tipo "smear" (estilo Neovide)
# El CRT va PRIMERO; el cursor smear se dibuja encima.
custom-shader = shaders/retro-crt.glsl
custom-shader = shaders/cursor-smear.glsl
# false = los shaders solo se redibujan cuando cambia la pantalla, no en bucle
# continuo: en reposo la GPU baja a 0 FPS (menos batería/calor). Coste: el
# cursor-smear pierde su animación fluida (el CRT estático no se ve afectado).
custom-shader-animation = false

keybind = shift+enter=text:\x1b\r

# Auto-start tmux
command = /opt/homebrew/bin/tmux new-session -A -s main-$RANDOM
