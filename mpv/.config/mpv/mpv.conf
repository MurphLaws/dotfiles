# yt-dlp: usar impersonación de navegador (curl_cffi) en el extractor genérico.
# Necesario para streams detrás del reto anti-bot de Cloudflare (p. ej. los
# m3u8 de senshi/ninstream que usa zigoku); sin esto yt-dlp recibe HTTP 403.
ytdl-raw-options-append=extractor-args=generic:impersonate
